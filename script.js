const header=document.querySelector('[data-header]');
const menuButton=document.querySelector('[data-menu-toggle]');
const mobileMenu=document.querySelector('[data-mobile-menu]');
const setMenu=open=>{mobileMenu.classList.toggle('open',open);document.body.classList.toggle('menu-open',open);menuButton.setAttribute('aria-expanded',String(open));menuButton.textContent=open?'Fechar':'Menu';mobileMenu.setAttribute('aria-hidden',String(!open))};
menuButton.addEventListener('click',()=>setMenu(!mobileMenu.classList.contains('open')));
mobileMenu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>setMenu(false)));
const updateHeader=()=>header.classList.toggle('scrolled',scrollY>24);addEventListener('scroll',updateHeader,{passive:true});updateHeader();
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
const form=document.querySelector('[data-interest-form]');const feedback=document.querySelector('[data-search-feedback]');const cards=[...document.querySelectorAll('[data-tags]')];
form.addEventListener('submit',event=>{event.preventDefault();const query=new FormData(form).get('interest').toString().trim().toLocaleLowerCase('pt-BR');cards.forEach(card=>card.classList.remove('is-match'));if(!query){feedback.textContent='Conte o que você quer viver para encontrar uma rota.';return}const terms=query.split(/\s+/);const matches=cards.filter(card=>terms.some(term=>card.dataset.tags.includes(term)));if(matches.length){matches.forEach(card=>card.classList.add('is-match'));matches[0].scrollIntoView({behavior:'smooth',block:'center'});feedback.textContent=`${matches.length} rota${matches.length>1?'s':''} combina${matches.length>1?'m':''} com a sua busca.`}else{document.querySelector('#planeje').scrollIntoView({behavior:'smooth'});feedback.textContent='Vamos começar pelas intenções de viagem.'}});
const suggestions={desacelerar:'Rota sugerida: Praia do Pedral, Massanori e entardecer à beira do Xingu.',aventura:'Rota sugerida: remada no Xingu, pesca esportiva e cachoeiras.',conectar:'Rota sugerida: orla de Altamira, sabores amazônicos e experiências culturais.'};
document.querySelectorAll('[data-plan]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-plan]').forEach(item=>item.classList.toggle('active',item===button));document.querySelector('[data-plan-result]').textContent=suggestions[button.dataset.plan]}));
